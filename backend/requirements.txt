# Web framework (updated 2026-04-22)
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.6

# AI / LLM
anthropic>=0.40
openai>=1.12

# PostgreSQL + pgvector (替换 ChromaDB)
psycopg2-binary>=2.9
pgvector>=0.3

# Embedding
sentence-transformers>=3.0

# Document parsing
PyPDF2>=3.0
python-docx>=1.0
chardet>=5.0

# Resume export (weasyprint 需要系统依赖, Railway 部署时可选)
# weasyprint>=60.0
markdown>=3.5

# Voice (iFlytek WebSocket)
websockets>=12.0

# Pydantic + settings
pydantic>=2.5
pydantic-settings>=2.1

# HTTP client
httpx>=0.26

# Web search
duckduckgo-search>=8.0

# Utilities
python-dotenv>=1.0

# Testing
pytest>=8.0
pytest-asyncio>=0.23
